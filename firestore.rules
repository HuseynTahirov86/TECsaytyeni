rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are insecure and allow anyone to read and write to your database.
    // This is a temporary solution for development and testing.
    // Before deploying to production, you must secure your database.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
